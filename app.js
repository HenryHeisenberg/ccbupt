import Notify from './dist/vant/notify/notify';

const utils = require("./utils/util.js");

App({
  data: {
    // host: "https://127.0.0.1",
    host: "http://127.0.0.1",
    student : {}
  },
  onLaunch: function() {
    /* 登录 */
    // utils.login()
    /* 登录 */
  }
})